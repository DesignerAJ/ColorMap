# AGENTS.md

**작업 맥락은 [`CLAUDE.md`](./CLAUDE.md) 에 있다. 코드를 만지기 전에 먼저 읽을 것.**

이 파일은 포인터다. 같은 내용을 두 곳에 두면 한쪽이 반드시 낡으므로,
결정의 근거·함정·환경 제약·남은 일은 전부 `CLAUDE.md` 한 곳에만 적는다.

특히 이런 걸 건드리기 전에는 반드시 읽어야 한다 —
CSS 캐스케이드(`@scope` 는 명시도를 올려주지 않는다), 레이어 순서,
스타일마다 다른 레이어 이름, 한글 IME, 지도 데이터 생성 파이프라인.
전부 코드만 봐선 알 수 없고, 모르고 고치면 조용히 깨진다.

**국경선과 국가 경계는 특히 조심할 것.** KBS 보도용이라 잘못 나가면 방송 사고다.
선과 색칠은 **반드시 같은 출처**여야 한다 — 한쪽만 Mapbox 로 바꿨다가 시도·시군구
색칠이 전부 선과 어긋난 적이 있다. 한강 하구·연평도 북쪽 섬·군사분계선을 만질 일이
생기면 `CLAUDE.md` 의 '결정과 근거'를 먼저 다 읽고, `test/data.test.js` 를 돌려볼 것.

## 최소한 알아야 할 것

- **빌드 단계가 없다.** 파일을 그대로 서빙하며, GitHub Pages 가 `main` 을 배포한다.
- `index.html` 과 `data/script.js` 의 `?v=` 는 캐시를 끊는 값이다. **총 8곳을 같은 값으로** 맞춘다.
- `recorder/js/data/` 의 생성물(`sido-hires.json` 등)을 손으로 고치지 말 것.
  도구를 고치고 다시 생성한 뒤 `node --test test/data.test.js` 로 검증한다.
- 새 의존성을 함부로 늘리지 말 것. 사내 npm 이 막혀 있어 테스트를 두 층으로 나눠 놓았다.

```bash
node tools/serve.mjs                  # http://127.0.0.1:5500
node --test test/*.test.js            # 59개, 설치 불필요
npm install && node --test test/dom/*.test.js   # 39개, jsdom 필요
```

## 이 저장소의 습관

변경 이유는 커밋 메시지에, 그 줄이 왜 그런지는 코드 주석에 남긴다.
어떤 코드가 이상해 보이면 `git log -p --follow <파일>` 을 먼저 보는 게 빠르다.
