# ColorMap — 작업 맥락

KBS 보도그래픽부용 지도 도구. 지역을 색칠하고 카메라 경로를 녹화해 MP4·PNG·PSD·SVG로 내보낸다.
**빌드 단계가 없다.** 파일을 그대로 서빙하며, GitHub Pages가 `main`을 배포한다.

> 이 파일에 넣을 것: **코드만 봐선 모르는 것** — 결정의 근거, 함정, 환경 제약, 남은 일.
> 변경 이유는 커밋 메시지에, 그 줄이 왜 그런지는 코드 주석에 남긴다. 여기까지 끌고 오면
> 매 세션 읽히는 파일이 비대해져 오히려 안 읽힌다. 자동으로 채워지지 않으니 직접 갱신할 것.

## 지금 상태

- 작업 브랜치 `dev-GUHNZ` (3.x). **배포된 `main`은 아직 2.0이다** — 3.x는 배포된 적 없다.
- 저장소는 **공개(public)**. `github.com/DesignerAJ/ColorMap`

## 구조

```
index.html          ?v= 로 캐시를 끊는다. CSS 2 + JS 5, 총 7곳
data/script.js      부팅: 토큰 받아 map 생성 → panel.html 주입 → initRecorder(map)
                    PANEL_URL 의 ?v= 도 index.html 과 같이 올려야 한다 (총 8곳)
recorder/panel.html 패널 UI 조각 (단독 페이지 아님)
recorder/js/config.js    스타일 목록, SEARCH_KEYS
recorder/js/recorder.js  거의 전부. 3,000줄이 initRecorder 클로저 하나에 들어 있다
data/style.css + recorder/css/app.css   순서대로 겹친다 (아래 '캐스케이드' 참고)
```

## 데이터 파이프라인

```
sigungu.json  ──(build-sido-hires.mjs)──>  sido-hires.json
                        + saemangeum.json (보충 폴리곤)
sido-hires.json ──(build-korea-border.mjs)──>  korea-border.json (북쪽 국경선)
OSM + korea-border.json ──(build-nk-admin1.mjs)──>  admin1.json 의 '북한' 13개
admin1.json + Natural Earth ──(build-admin1-split.mjs)──>  admin1-meta/-core/admin1/*.json
geoBoundaries ──(build-admin1-hires.mjs)──>  admin1/*.json (split 뒤)
OSM ──(build-admin1-osm.mjs)──>  admin1/*.json 의 개별 표기 대상국 (hires 뒤)
OSM ──(build-admin1-firstlevel.mjs)──>  admin1.json 의 프랑스·이탈리아·스페인 (split 앞)
```

- 생성물을 손으로 고치지 말 것. 도구를 고치고 다시 생성한다.
- `build-korea-border.mjs` 는 `MAPBOX_TOKEN` 과 `npm i @mapbox/vector-tile pbf` 가 필요하다.
- `build-nk-admin1.mjs` 는 의존성이 없다. `admin1.json` 의 **'북한'만** 갈아끼고 나머지 4,579개는
  그대로 둔다 — 통째로 다시 만드는 도구가 아니다.
- 재생성하면 `node --test test/data.test.js` 로 반드시 검증한다.

## 환경 제약

- **사내 npm 이 인증서에 막혀 설치가 안 된다.** 그래서 테스트를 두 층으로 나눴다 —
  `test/` 는 의존성 0, `test/dom/` 은 jsdom 필요(CI에서 설치). 새 의존성을 함부로 늘리지 말 것.
- 같은 인증서 때문에 **node 의 `fetch` 도 막힌다**(`SELF_SIGNED_CERT_IN_CHAIN`). `curl` 은
  시스템 키체인을 써서 통과하므로, 외부를 부르는 도구는 `fetch` 실패 시 `curl` 로 넘어가게
  써 두었다(`build-nk-admin1.mjs`). 인증서 검증을 끄는 선택은 하지 않았다.
- **Overpass 는 User-Agent 를 안 밝히면 406 으로 막는다.** node 의 `fetch` 기본 UA(`node`)도
  막힌다 — `curl` 만 통과해서 오래 '사내망 탓'으로 보였다. 응답이 HTML 안내문이라 혼잡과
  구별도 안 됐다. 도구들은 UA 를 밝히고, 재시도 로그에 **실제 오류**를 찍는다. 남은 504 는
  진짜 부하라 다음 시도에 200 이 온다(재시도 8번).
- 이 맥을 만든 환경에선 `python3` 이 깨져 있었다(Command Line Tools). `node tools/serve.mjs` 를 쓴다.
- **검색 인증키는 브라우저에 노출되는 값이다.** 도메인·리퍼러 제한이 유일한 방어선.
  - VWorld: 검색 API 는 type 마다 category 가 필요한 것이 있다 — place 는 없어도 되지만
    district·address 는 **필수**라 안 주면 PARAM_REQUIRED 로 떨어진다. 한동안 address 폴백이
    그래서 늘 실패했고 콘솔에 인증키 문제라고 잘못 떴다.
    district(category L1/L2/L3)는 **국내 행정지명만** 준다 — 파리·런던·도쿄는 0건이라
    한글 질의가 국내인지 해외인지 가르는 잣대로 쓴다.
  - VWorld: 사용 도메인에 `127.0.0.1` 과 `designeraj.github.io` 둘 다 등록해야 한다.
    로컬에서 `등록되지 않은 인증키입니다` 가 뜨면 대개 이것.
  - Google: 키를 넣고 HTTP 리퍼러 제한을 걸었다. **리퍼러 패턴을 경로까지 좁히지 말 것** —
    브라우저가 교차 출처 요청에 출처만 보내서 `…/ColorMap/*` 로 걸면 전부 막힌다.
    `https://designeraj.github.io/*` 와 `http://127.0.0.1:5500/*` 두 줄이면 된다.
    Text Search 는 좌표를 받으려면 Pro SKU 라 월 5,000건 무료, 초과 시 1,000건당 약 $32.
    필드마스크에 Enterprise 필드를 넣으면 단가가 뛴다.
  - 키를 저장소에 그대로 두기로 했다 (공개 사이트라 어차피 노출되고, 도메인 제한이 실질 방어).

## 이 코드베이스의 함정

**CSS 캐스케이드.** `data/style.css` 에 2.0 시절 규칙이 남아 있고 ID 를 끼고 있어 명시도가 높다.
`app.css` 는 `@scope` 안에 있는데 **`@scope` 는 명시도를 올려주지 않는다.** 그래서 클래스 규칙이
`#country-selector-container label` 같은 규칙에 진다. 패널 라벨 34개가 통째로 16px 로 그려지던
사고가 여기서 났다. 라벨 크기를 만질 땐 `test/dom/cascade.test.js` 를 먼저 돌려볼 것.

**핀치(자기접촉 링).** 한 링이 같은 점을 두 번 지나면 mapbox-gl 의 삼각분할이 그 점을
가로질러 이어 **화면에 얇고 긴 삼각형이 뻗는다.** 줌에 따라 나타났다 사라진다.
정밀도를 줄이거나 해안선으로 자르는 과정에서 새로 생긴다 — 실제로 충남 해안에서 났다.
경계 데이터를 만드는 도구는 전부 `splitAtPinches` 로 링을 나눠 내보내고,
`test/data.test.js` 의 '국가 폴리곤과 행정구역 폴리곤에 핀치가 없다' 가 막는다.

**자기교차는 핀치와 다른 문제다.** 꼭짓점을 공유하지 않고 두 변이 그냥 가로지르는 경우로,
1m 안팎으로 되돌아오는 슬리버가 원인이다. 핀치 검사에는 안 걸리는데 증상은 똑같다 —
충남 해안 삼각형의 진짜 원인이 이거였고, 시도 17개에 85곳 있었다.
`build-sido-hires.mjs` 의 `cleanRing` 이 교차 복구와 핀치 분리를 **번갈아** 돌린다
(교차를 자르면 핀치가 생기고, 핀치를 나누면 또 교차가 드러난다).

**구멍이 바깥 링에 맞닿아도 같은 삼각형이 난다.** mapbox-gl 이 구멍을 바깥 링에 잇는 다리를
놓다가 폭 0 인 삼각형을 만든다. 안산·시흥 해안(경기 126.655, 37.216)이 그랬고 26곳 있었다.
맞닿은 점을 구멍 안쪽으로 약 2m 민다(`tools/lib/rings.mjs`). **미는 폭이 좌표 정밀도보다
커야 한다** — 1e-6 으로 밀었더니 하위 도구의 5자리 반올림이 원래 자리로 되돌려 놓았다.
반올림이 없던 겹침을 새로 만들기도 해서, 각 도구가 자기 출력에 대해 한 번 더 돌린다.

**여섯 번째: 해안선 잇기 실패를 직선으로 때우면 안 된다.** 강 하구에서는 두 교차점이 서로 다른
해안선 고리에 놓여 이을 길이 없다. 예전엔 직선으로 때웠고, 두만강 하구에 21.5km 짜리 수평선이
생겨 그 선과 해안 사이가 통째로 칠해졌다. 원래 경계를 그대로 두는 편이 낫다 — 바다로 조금
나가더라도 없는 땅을 만들어내지는 않는다.

**여덟 번째는 데이터가 아니다 — `tolerance: 0`.** 위 일곱은 전부 지오메트리 문제라
`test/data.test.js` 가 잡는다. 여덟 번째는 **검사에 아무것도 안 걸리는데** 화면에만 뻗는다.
mapbox-gl 은 타일 좌표를 정수로 반올림하는데(extent 8192 = 0.0625 px 격자),
`tolerance: 0` 은 단순화를 **끄는** 값이라 격자보다 촘촘한 점이 전부 살아남아
같은 칸으로 내려앉는다. 링이 스스로를 훑는 모양이 되고 삼각분할이 튄다.

**가려내는 법: SVG 로 뽑아 본다.** SVG 만 `queryRenderedFeatures()` 의 지오메트리를
직접 path 로 그리고, 화면·MP4·PSD 는 셋 다 캔버스를 굽는다. **SVG 만 멀쩡하면
지오메트리는 끝까지 정상이고 래스터화에서만 깨지는 것** = 데이터를 파도 안 나온다.
함경남도에서 이걸 모르고 데이터만 오래 팠다.

`tolerance` 의 단위는 **CSS 픽셀**이다(mapbox-gl 이 ×16 해서 타일 단위로 바꾼다).
격자 0.0625 px 보다 크기만 하면 된다. `GEO_TOLERANCE = 0.125` 로 두었다 —
mapbox 기본값(0.375)보다 3배 촘촘하고 격자의 2배다. **선과 색칠에 같은 값을 써야 한다.**

삼각형의 원인이 **여덟**이고 증상이 똑같다 — 분기점 조립, 핀치, 자기교차, 맞닿은 구멍,
구멍 안의 구멍, 해안선 잇기 실패, 감김 방향, 그리고 `tolerance: 0`.
하나를 고쳐도 안 없어지면 나머지를 의심할 것. **데이터 검사가 전부 초록이면 여덟 번째다.**

**벡터 타일에는 '구멍' 이라는 표시가 없다 — 감김 방향이 유일한 기준이다.**
GeoJSON 에서 링을 폴리곤마다 따로 내보내면 위상적으로는 맞지만, 타일로 구우면 링이 한 줄로
늘어서고 mapbox-gl 은 **첫 링의 부호를 바깥으로 잡은 뒤 부호가 반대인 링을 전부 앞 폴리곤의
구멍으로** 붙인다. OSM 은 호수·섬을 본토와 반대 방향으로 감아 두는데, 그걸 그대로
`rings.map(r => [r])` 로 내보냈더니 함경남도는 링 165개 중 164개가 통째로 본토의 구멍이 됐다.
겹치고 맞닿은 구멍 164개 → 화면에 긴 다각형이 뻗었다(128.0, 40.04). 이게 **삼각형이 아니라
다각형으로 보이면** 삼각분할이 튄 게 아니라 구멍이 잘못 뚫린 쪽을 먼저 의심할 것.
링 분류·감김 정리는 `tools/lib/rings.mjs` 의 `buildPolygons` 하나로 모아 뒀다.
`test/data.test.js` 의 '링 감김 방향이 GeoJSON 규약대로다' 가 막는다.

**어느 레이어가 그리는지부터 확인할 것.** 브라우저 콘솔에서
`map.queryRenderedFeatures(점)` 를 찍어보면 된다. 삼각형 안쪽인데 fill 레이어가 안 나오면
= 폴리곤은 그 자리를 안 덮는데 화면에만 그려지는 것 = 삼각분할 문제로 확정된다.
이 한 줄이 헛짚기를 끝냈다.

**구멍 안의 구멍은 구멍이 아니다.** 호수 속의 섬이라 육지다. "바깥 링 안에 있으면 구멍"으로
분류하면 구멍 둘이 겹쳐 놓여 같은 삼각형이 난다 — 충남 부사호(126.560, 36.470)가 그랬다.
링을 **중첩 깊이**로 분류한다: 짝수면 육지, 홀수면 구멍. 부모는 자기를 감싸는 링 중 가장 작은 것.
구멍끼리 꼭짓점을 공유해도 같은 증상이 난다(10곳 있었다).

**거대한 feature 하나로 뭉치지 말 것.** 국가 색칠을 만들 때 시도 4,173개 폴리곤을
MultiPolygon 하나에 몰아넣었더니 같은 삼각형이 났다. 지오메트리는 시도 탭과 똑같은데
feature 하나가 너무 커서 타일마다 삼각분할이 깨진 것이다. **시도·도마다 feature 를
나눈다.** 색은 속성으로 match 하므로 나뉘어도 한 나라가 한 색으로 칠해진다.

**색칠은 탭을 바꿔도 남는다.** 국가와 행정구역을 같이 쓰려고 일부러 그렇게 뒀다.
지우려면 각 탭의 '전체 삭제'. 예전엔 전환할 때마다 초기화라 하던 작업이 날아갔다.

**레이어 순서.** 색칠은 첫 symbol 레이어 바로 아래에 깔고, 경계선을 그 위로 끌어올린다
(`raiseBoundaries`). 끌어올릴 대상은 **이름이 아니라 소스**(`admin`·`country_boundaries`)로 고른다 —
이름으로 나열했더니 8개 중 5개를 놓쳐 시군구 경계선이 색칠에 덮였다.

**스타일마다 레이어 이름이 다르다.** 같은 국경선이 `country_border`·`country-border`·
`admin-0-boundary-bg` 로 제각각이다. 새 스타일을 추가하면 실제 레이어 목록을 먼저 확인할 것.

**정수 줌에 걸터앉으면 줌아웃이 없어도 타일이 깜빡인다.** mapbox 는 정수 줌마다 다른 타일을
쓴다(벡터는 `Math.floor`, 래스터만 `round`). 줌이 정수에 정확히 걸리면 카메라 줌을 곡선
공식으로 계산하는 과정의 부동소수점 오차만으로 floor 가 뒤집힌다 — 실측 7.999999999988.
타일이 통째로 바뀌어 도로·지형 음영이 있다 없다 하고, **콘솔에 줌을 찍으면 내내 8.000 이라**
원인이 안 보인다. `Math.floor(map.getZoom())` 을 찍어야 보인다.
그래서 출발·도착 줌이 경계에 붙어 있으면 떼어 놓는다 — 줌아웃이 있으면 아래 칸 꼭대기로
(8.0 → 7.99, 정지 화면과 이동 초반이 같은 칸), 없으면 같은 칸 안쪽으로(8.0 → 8.01).
이동 **중**에 경계를 넘는 건 문제가 안 된다 — 카메라가 움직이고 있어서 눈에 안 띈다.
떼어낸 카메라는 **녹화가 쓰는 모든 자리**에 적용해야 한다 — 출발·도착·경유지·앞뒤 정지·
프리로드 전부. 뒤 정지만 원본을 쓰고 있어서 도착에서만 튀었다.

**경로선과 카메라는 같은 좌표를 봐야 한다.** 아크를 그려 놓고 카메라가 최단거리로 가면
화살표가 화면 밖으로 나간다. 줌은 비행 곡선 그대로 두고 **중심만 경로 위**를 지나게 한다.
세 가지가 **같은 경로**를 봐야 한다 — 그려지는 선 · 카메라 · **타일 프리로드**.
프리로드가 어긋나면 이동 중에만 저해상도 타일이 그려져 지도 디테일이 확확 바뀐다.
다만 **카메라는 `trimArcEnd` 로 자르기 전 경로**를 따라야 한다 — 자르기는 선을 도착핀 앞에서
멈추려는 것이지 카메라를 멈추려는 게 아니다. 잘린 끝까지만 따라가면 마지막 프레임에서
도착 카메라로 점프해 화면이 튄다. 선은 잘린 것으로 그리고, 카메라와 프리로드는 원본을 본다.
실시간 녹화는 경로를 따라갈 때만 flyTo 대신 프레임 단위로 몬다 — flyTo 는 자기 곡선으로만
움직여서 임의의 경로를 못 따라간다.

**IME.** 한글 조합 중 Enter 는 글자 확정용이라 무시해야 한다. 동시에 목록에서 방향키로 고른 값은
Enter 의 **기본 동작이 끝난 뒤에야** 입력창에 들어온다 — 그래서 `setTimeout(…, 0)` 으로 미뤄 읽고
`preventDefault` 는 하지 않는다.

**패널 글자 대비.** 카드 바탕은 `흰색 70% → 반투명 패널 → 지도` 로 겹쳐 있어 **실제 배경이
지도에 따라 달라진다.** 그래서 대비 수치는 근사치다. 그래도 눈에 띄게 안 보이는 값은 걸러야 하니
`test/dom/cascade.test.js` 가 팔레트를 검사한다 — 완료 표시(`--ok`)는 4.5:1, 나머지는 3:1 기준.
색을 새로 정할 땐 그 테스트를 먼저 돌려볼 것. `--ok` 가 한때 1.84:1 이라 '지정됨'이 안 읽혔다.

## 결정과 근거 (되돌리기 전에 읽을 것)

- **새만금**은 국토부 VWorld 의 시군구·시도 레이어 **양쪽 다** `NOT_FOUND` 다. 어디에도 배정돼
  있지 않다. 시도 단위로는 전북이 분명하므로 별도 폴리곤으로 전북에 얹었다.
  **매립지만** 넣고 새만금호 수면(186km²)은 뺐다 — 통째로 채우면 바다를 칠한 것처럼 보인다.
  출처는 OSM(ODbL)이라 `README` 와 `saemangeum.json` 에 출처를 밝혀 두었다.
- **북쪽 국경선**은 Mapbox 대신 우리 시도 데이터에서 뽑아 그린다. Mapbox 의 KP-KR 선은 우리
  행정경계와 최대 3.1km 어긋나고 줌을 올려도 안 줄어든다(= 단순화가 아니라 데이터가 다름).
  국토부 경계와는 중앙값 6m 로 일치하며, 어긋나는 20곳은 전부 VWorld 가 DMZ 를 빼서 생긴 차이였다.
  Mapbox 쪽 KP-KR 구간은 `iso_3166_1 != 'KP-KR'` 로 가린다.
- **북한 행정구역**은 OSM 에서 다시 받아 `admin1.json` 에 갈아끼웠다(11 → 13개, 개성·남포 추가).
  원래 것은 도당 100~220점이라 우리 시도(1.6만점) 옆에서 각졌고, 군사분계선이 경기·강원과
  중앙값 1.75~6.6km 어긋났다. **군사분계선 구간은 `korea-border.json` 으로 치환한다** —
  같은 선을 두 데이터가 각자 그리면 아무리 정밀해도 또 어긋나기 때문이다.
  양 끝 해안 6km 는 예외다(해안선은 국토부 ↔ OSM 이라 원래 다르다). 되돌리기 전에
  `test/data.test.js` 의 '군사분계선이 북한 경계와 꼭짓점 단위로 붙는다' 를 볼 것.
- **OSM 행정경계는 영해까지 포함한다.** 처음 갈아끼웠을 때 황해남도를 칠하니 서해가 통째로
  칠해졌다. 해안선을 따로 받아 잘라낸다. **`maritime=yes` 태그로 거르면 안 된다** —
  태그 없는 way 도 바다를 지나서, 태그만 믿으면 '육지' 끝점이 해안에서 22km 떨어진 데 찍힌다.
  꼭짓점마다 해안선 안쪽인지 직접 판정한다. 판정은 **동쪽 광선**으로 한다 — 해안선을 닫힌
  고리로 이으면 bbox 에서 잘린 탓에 내륙 4개 도가 통째로 '바다'로 나온다.
  자르면 본토만 남으므로 섬(갈도·장재도·무도 등)을 되붙이는 단계가 따로 있다.
- **국가 색칠은 남·북한만 우리 데이터**(`korea-countries.json`)로 그린다. Mapbox 가
  갈도·장재도·무도·료도를 KOR 로 줘서 대한민국을 칠하면 북한 섬이 함께 칠해졌고,
  선·시도 색칠(국토부)과 최대 3.1km 어긋났다. **선과 색칠은 같은 출처여야 한다** —
  2.0 이 맞아 보였던 건 시도 색칠이 OSM 이고 Mapbox 행정경계도 OSM 이라 우연히 맞은 것이다.
  한때 국경선을 Mapbox 로 되돌렸다가 시도·시군구가 어긋나 곧바로 되돌렸다.
- **한강 하구**는 우리 데이터로 만들 수 없다. `build-korea-border.mjs` 는 경기·강원만 보는데
  강화·교동·석모도는 **인천광역시**라, 선이 126.53°E 에서 끊기고 염하(김포–강화 사이)로 꺾여
  내려갔다. 인천을 넣어도 안 된다 — 우리 데이터로 나오는 건 강화·교동의 **해안선**이지 하구
  한가운데를 지나는 선이 아니다. 애초에 하구엔 군사분계선이 없다(정전협정상 중립수역).
  그래서 육상은 우리 선, 하구부터 서쪽은 OSM 의 KP-KR 선을 이어 붙인다(`kind: "estuary"`).
  물 위라 어느 색칠과도 맞물리지 않으므로 출처가 달라도 어긋나 보이지 않는다.
  서쪽 끝은 **말도**(강화군 서도면)에서 끊는다 — 중립수역이 끝나는 곳이고, 그 서쪽은 NLL 이라
  성격이 다른 선이다. 같은 굵기로 이어 그리면 확정된 국경처럼 읽힌다.
  동쪽도 같은 문제였다 — 군사분계선은 고성에서 동해에 닿으며 끝나는데 선이 거기서 멈추지 않고
  **해안을 따라 5.83km 더 남쪽으로** 내려가, 그만큼의 해안이 북한 쪽에 놓인 것처럼 보였다.
  강원도 경계의 최북단에서 자른다. 자를 때는 **자유로운 끝에서만** — 조각끼리 맞물리는
  이음매를 자르면 선이 끊긴다(처음에 그렇게 6점을 잘라 끊었다).
  그 종점은 VWorld 가 DMZ 를 빼서 해안선보다 227m 안쪽이라, 방위 60°(2시 방향)로
  해안선까지 잇는 조각을 따로 둔다(`kind: "shore"`). 우리 시도 데이터가 아니라서
  '국경선이 색칠과 꼭짓점 단위로 붙는다' 검사에서 빠져야 하기 때문이다.
  `build-nk-admin1.mjs` 는 하구 조각을 **빼고** 써야 한다.
- **행정구역선도 남·북한은 우리 데이터**(`korea-admin1-lines.json`)로 그린다. 스타일이 주는
  `admin-1-boundary` 는 우리 색칠(시도 1.6만점, 북한 도 4~8천점)보다 훨씬 성겨 모양이 안 맞았다.
  **맞닿은 변만** 뽑는다 — 폴리곤 외곽선을 통째로 그리면 해안선까지 행정구역선이 되어
  나라 둘레에 테두리가 생긴다. 두 번 나오는 변이 이웃과 맞댄 경계, 한 번은 해안선이다.
- **행정구역 데이터는 나라별로 나눠 받는다.** 전 세계를 한 파일(전송 10.7MB)로 받던 것을
  `build-admin1-split.mjs` 가 셋으로 나눈다 — 속성만 담은 `admin1-meta.json`(0.11MB),
  자주 쓰는 8개국 `admin1-core.json`(7.2MB), 나머지 227개국 `admin1/<ISO3>.json`(중앙값 14KB).
  **받은 건 버리지 않는다** — 227개국을 다 받아도 6.7MB 라 예전 한 번 로드보다 적다.
  LRU 를 넣으면 이미 칠한 구역이 사라지는 걸 따로 막아야 해서 오히려 위험하다.
  코어 8개국(한국·북한·일본·중국·미국·러시아·우크라이나·대만)은 **원본 그대로 둔다** —
  한국·북한은 우리 고해상도이고 러시아·일본·우크라이나는 원본이 이미 Natural Earth 보다 정밀하다.
  미국(398점)·중국(661)·대만(1,075)은 NE 가 2.5배 정밀하지만 "지금대로" 요청이라 뺐다.
  바꾸려면 도구의 `KEEP` 에서 빼면 된다.
- **나머지 224개국은 Natural Earth 10m 으로 올렸다** — 구역당 중앙값 73 → 166점.
  프랑스 90 → 227, 독일 209 → 903. 나라별로 **더 정밀한 쪽을 고른다** — 통째로 갈아끼우면
  일본·러시아·이란·캐나다가 오히려 나빠진다. 구역끼리 짝짓기는 이름이 아니라 **무게중심**으로
  한다(표기가 달라 이름으로는 조용히 어긋난다). 속성(한글 이름·카메라 목표)은 우리 것을 지키고
  지오메트리만 바꾼다. 출처는 Natural Earth(퍼블릭 도메인).
- **주요국 66곳은 geoBoundaries 로 한 단계 더 올렸다.** Natural Earth 로도 프랑스가
  227점이라 우리 시도(2.3만점) 옆에서는 여전히 각졌다. geoBoundaries 는 각국 공식 기관
  데이터라(프랑스 IGN·독일 BKG) 자릿수가 다르다 — 원본은 프랑스 전송 26MB 다.
  **줌 10 에서 0.125px** 를 넘지 않게 줄인다. 그 값의 근거는 위 `tolerance` 항목이다 —
  타일 반올림 격자가 0.0625px 이므로 그보다 크면서 안 보이는 선을 고른 것이다.
  **행정 단위가 나라마다 다르다.** 우리 독일은 16개 주(ADM1)인데 프랑스는 101개
  데파르트망(ADM2), 이탈리아는 110개 도(ADM3)다. 레벨을 박지 말고 구역 수로 고를 것.
  짝짓기는 **우리 폴리곤 안에 상대 무게중심이 들어가는가**로 한다 — 이름은 표기가 다르고
  거리는 작은 구역이 이웃에 붙는다. 절반도 못 붙으면 그 나라는 건드리지 않는다.
  노르웨이·헝가리·아일랜드·필리핀·케냐·뉴질랜드는 행정 개편·단위 차이로 짝이 없어 그대로다.
  **구역 수가 맞는다고 더 정밀한 건 아니다** — 이란(3,831점)·아르헨티나(447)·파키스탄(846)은
  geoBoundaries 쪽이 오히려 거칠어서 건너뛴다. 도구가 직접 막는다.
  `test/data.test.js` 는 `admin1.json` **원본**과 비교하므로 이걸 못 잡는다 —
  원본보다는 나은데 지금보다 나쁜 상태는 그대로 통과한다.
  **나라당 전송 1.5MB 상한**이 있다. 넘으면 기준 줌을 낮춘다 — 칠레는 피오르 해안이라
  줄이기 전 6MB 였다. 캐나다는 원본이 512MB 를 넘어 node 가 문자열로 못 읽는다(건너뛴다).
  **이 도구를 연달아 두 번 돌리면 안 된다** — 덮어쓴 자기 출력을 다시 읽어 전부 건너뛴다.
- **출처 표기가 이미지에 따라붙는다.** 지도를 PNG·PSD·MP4 로 뽑으면 그 데이터의 표기 의무가
  결과물에 붙는다(ODbL 은 조항에 명시). 한 그래픽에 세 나라를 칠하면 기관 셋을 밝혀야 한다는
  뜻이라, **출처 개수 자체가 비용**이다. 바탕 지도가 Mapbox(OSM)라 `© OpenStreetMap
  contributors` 는 어차피 표기하므로, 행정구역도 OSM 이면 표기가 늘지 않는다.
  그래서 개별 표기가 필요했던 39개국을 OSM 으로 바꿨다 — **남은 건 9개국**이다.
  **조악해지지 않는다** — 프랑스 원본은 IGN 29,985점/구역, OSM 20,945점이지만 화면에 쓰는
  정밀도(z8)까지 줄이면 IGN 2,259 · OSM 2,602 로 OSM 이 오히려 많다. 서브픽셀까지 줄이면
  꼭짓점 수는 원본 밀도가 아니라 그 축척의 형태 복잡도로 정해진다.
- **어느 나라가 어느 출처인지는 `admin1-sources.json` 에 있다.** 도구 셋이 차례로 덮어쓰는
  구조라 출처가 실행 순서에만 남아 있었고 README 가 어긋나기 쉬웠다. 이제 파일을 쓰는 도구가
  `lib/sources.mjs` 로 그때그때 적는다. 사람이 옮겨 적지 않는다.
- **영해는 육지로 잘라낸다** (`build-admin1-clip.mjs` · `lib/land.mjs`). OSM 행정경계가
  영해를 포함하는 나라가 있어서 바다가 칠해졌다 — 브라질 리우데자네이루는 넓이의 **30.8%**
  가 바다였다. 나라 합계 넓이로는 안 잡힌다(브라질 1.03배). 구역별 넓이로도 못 가른다 —
  벨기에 브뤼셀이 13배인데 그건 **내륙**이고 도시 경계 정의가 달랐던 것이다.
  육지는 Natural Earth 10m(퍼블릭 도메인)을 쓴다. 전 세계가 닫힌 폴리곤이라
  OSM 해안선처럼 bbox 에서 잘려 내륙이 '바다'로 나오는 문제가 없다.
  **아주 조금 줄면 자르지 않는다**(1% 문턱). NE 해안선이 우리 경계보다 거칠어서 영해가
  없는 나라도 0.1~0.7% 씩 깎인다 — 프랑스는 원래 바다를 안 덮는데 0.4% 가 깎였다.
  실제 영해는 자릿수가 다르다(미얀마 2.4% · 독일 6.8% · 튀르키예 7.6% · 리우 30.8%).
  **해안선 조각에 길이 상한이 있어야 한다** — 육지 링 하나가 남미 대륙 전체이기도 해서,
  짧은 쪽으로 돌아도 대륙을 반 바퀴 도는 경우가 있다. 브라질이 4,090배가 됐다.
  직선거리의 25배까지만 따라간다.
  **강 하구는 못 자른다.** 나가는 지점과 들어오는 지점이 서로 다른 육지 덩어리에 놓여
  이을 길이 없다(아마존 하구의 파라). 원래 경계를 그대로 둔다 — 직선으로 때우면 없는 땅이 생긴다.
  **섬을 지우지 말 것.** 꼭짓점이 전부 바다로 나오면 대개 NE 10m 에 없는 작은 섬이다.
  한 번 버렸다가 덴마크 690개 · 프랑스 1,599개 폴리곤이 사라졌다 — 화면에서는 섬이
  색칠 안 되는 것으로 보인다. 모르는 섬을 지우느니 영해 조각이 남는 편이 낫다.
  **폴리곤 구조도 그대로 둘 것.** 링을 전부 모아 buildPolygons 로 다시 나누면, 자른 본토
  링이 만을 가로지르며 그 안의 섬을 감싸 **중첩 깊이로는 구멍**이 된다(덴마크 83개·
  이탈리아 61개). 바깥 링만 자르고 구멍(호수)은 건드리지 않는다.
  **자른 뒤에는 반드시 복구한다** (`lib/clean.mjs` 의 cleanRing). 해안선 조각을 끼워 넣으면
  링이 자기 자신과 만나 핀치·자기교차가 생기고, mapbox-gl 의 삼각분할이 깨져 **일부가
  아예 안 그려진다** — 마라냥에서 섬이 색칠 안 되는 것으로 보였다(핀치 11·교차 35).
  **바깥 링만이 아니라 구멍도 복구해야 한다** — 칠레 아이센의 교차 4곳이 전부 구멍에 있었다.
  복구는 자르기와 대상이 다르다. 자르는 건 OSM 에서 받은 나라뿐이지만(다른 출처는 이미
  육지만이다) 복구는 227개국 전부 한다 — Natural Earth 쪽에도 800곳 가까이 있었다.
  남는 한계: NE 10m 해상도라 좁은 연안은 못 잡는다(마르마라해 부르사 앞 13km).
- **OSM 행정경계의 영해 문제는 보편적이지 않다.** 북한은 바다까지 그려져 있어 해안선으로
  잘라내야 했지만, 프랑스 데파르트망 99개는 바다를 한 곳도 안 덮는다. 그래서 클리핑 대신
  **넓이로 검사해 걸러낸다** — 지금 데이터는 육지만이라 OSM 쪽이 10% 넘게 크면 바다다.
  그리스(1.81배)·카타르·아제르바이잔·칠레·스웨덴·파푸아뉴기니가 걸려 손대지 않았다.
- **ISO3166-2 는 ISO2 접두사를 쓴다** (프랑스 FR-59). ISO3 앞 두 글자를 그대로 쓰면
  AUT→AU 는 호주, MEX→ME 는 몬테네그로, CHL→CH 는 스위스, ARM→AR 은 아르헨티나,
  ISR→IS 는 아이슬란드다. 한 번 그렇게 짜서 7개국이 엉뚱한 나라를 받았다.
  넓이·짝짓기 검사가 전부 걸러내 데이터는 안 망가졌지만, 그 검사들이 없었으면 조용히 지나갔다.
  매핑은 OSM 국가 관계에서 받는다(`ISO3166-1` 과 `ISO3166-1:alpha3`).
- **탭 이름은 '행정구역'(1급)인데 원본 데이터는 나라마다 층이 달랐다.** 프랑스 101개
  데파르트망(2급)·이탈리아 110개 도(2급)·스페인 52개 provincia(2급). 전 세계를 훑어
  실질적으로 어긋난 건 이 셋과 기니·부르키나파소·스리랑카뿐이었고, 앞의 셋만 1급으로 바꿨다
  (18·20·19개). **구역 목록 자체가 바뀌는 일**이라 파생 파일이 아니라 원본 `admin1.json` 을
  고쳐야 한다 — 안 그러면 다음 split 에서 되살아난다.
- **선은 아직 Mapbox 다.** 색칠을 아무리 올려도 출처가 다르면 정확히는 안 맞는다.
  맞추려면 남·북한처럼 **선을 우리 폴리곤에서 뽑아야** 한다(`korea-admin1-lines.json` 방식).
- **행정구역 탭의 '강원도(북한)' 는 보이는 이름만 바꾼 것이다**(`ADMIN1_RENAME`). 데이터의
  `short` 를 고치면 정식명이 `북한 ${short}` 로 조립돼 있어(`build-nk-admin1.mjs`)
  '북한 강원도(북한)' 이 된다. 남북 도의 두 글자 약칭(충북·평남)도 같은 자리에서 만드는데
  (`sidoAbbr`), **'도' 로 끝나는 외국 이름이 141개**(홋카이도·코모도…) 있어 남북 것에만 쓴다.
  겹치는 '강원' 은 비워 둬 아무것도 안 골라지게 했다 — 후보 목록에서 고르는 편이 낫다.
- **위성 스타일**에서는 `landColor`·`water` 를 건드리지 않는다. 디자이너가 `visibility:none` 으로
  꺼둔 반투명 오버레이라, 켜면 사진 위에 얹혀 바다가 뿌예진다.
- **강·호수**는 바다와 색·불투명도를 맞춘다(`matchWaterToSea`). 스타일의 `water` 는 색은 같은데
  `fill-opacity: 0.5` 라 육지가 비쳐 옅어 보였다. 그 뒤로 꺼둘 이유가 없어져
  `RIVER_DEFAULT_OFF` 를 비웠다 — 물 레이어가 있는 스타일은 모두 켜진 채로 시작한다.

## 실행과 검증

```bash
node tools/serve.mjs                  # http://127.0.0.1:5500
node --test test/*.test.js            # 93개, 설치 불필요
npm install && node --test test/dom/*.test.js   # 53개, jsdom 필요
```

푸시하면 `.github/workflows/qa.yml` 이 둘 다 돌린다 (배포는 건드리지 않는다).
테스트가 막고 있는 실제 사고 목록은 `test/README.md` 에 있다.

**커밋 메시지에 왜 그렇게 했는지가 적혀 있다.** 어떤 코드가 이상해 보이면
`git log -p --follow <파일>` 을 먼저 보는 게 빠르다.

## 이어서 할 일

0. **브라질 마라냥의 섬이 화면에 안 칠해진다 — 미해결.** (2026-08-25)
   눈으로는 섬이 색칠 안 된 채로 보인다. **데이터 문제는 아닌 것으로 보인다** —
   아래를 전부 확인했고 다 깨끗하다. 다시 볼 때 같은 곳을 두 번 파지 말 것.

   확인해서 **배제된 것**:
   - 폴리곤이 그 자리를 덮는다. 상루이스섬(-44.30, -2.53)·카랑게주스섬(-44.85, -2.20)
     둘 다 `칠해짐`으로 나온다 (구멍도 아니다)
   - 핀치·자기교차 0 (227개 파일 전부, `test/data.test.js` 가 검사한다)
   - 폴리곤 수가 자르기 전후로 안 줄었다
   - 감김 방향은 buildPolygons/wind 로 맞춰 둔다
   - 영해 자르기가 섬을 지우지 않는다 (NE 에 없는 섬은 그대로 둔다)

   **다음에 볼 것** — 화면에서 확인해야 하는 것들이다:
   - `map.queryRenderedFeatures(점)` 로 그 섬 위에서 `admin1-color-fill` 이 나오는지.
     안 나오면 폴리곤은 덮는데 화면에만 없는 것 = 삼각분할 (위 '여덟 번째' 참고)
   - `map.showLayers2DWireframe = true` 로 삼각형 그물을 직접 볼 것
   - 줌을 바꿔가며 나타났다 사라지는지 (타일 경계 문제인지)
   - 마라냥은 **폴리곤 1개짜리 링 하나**로 본토와 섬을 잇고 있다. 영해를 자르면서
     본토와 섬이 해안선을 따라 아주 가는 통로로 이어졌을 수 있다. 그 통로가 타일
     좌표로 반올림되면서 폭 0 이 되면 삼각분할이 깨진다 — 링을 섬마다 나누는 쪽이
     맞을 수도 있다 (지금은 OSM 이 준 링 구조를 그대로 쓴다)

0. **북한 데이터 재생성 — 끝났다. 브라우저 확인만 남았다.** (2026-08-25)
   생성물을 다시 만들었고 `node --test test/*.test.js` 는 **67/67 초록**이다.
   막혀 있던 이유는 사내망이 아니라 **Overpass 의 User-Agent 차단**이었다(406) —
   재시도 로그가 그걸 '혼잡'으로 찍어 오래 헛짚었다. 도구를 고쳤으니 어디서든 돌아간다.
   브라우저에서 **북한·함경남도를 칠하고 줌 7 부근**을 확인할 것(줌 8 에서는 원래 안 보였다).
   호수가 구멍으로 뚫려 보이면 그건 정상이다 — 남한 시도도 그렇게 되어 있다.

1. **VWorld 인증키를 운영용으로 바꿔야 한다.** 지금 `recorder/js/config.js` 에 든 키는
   **개발용**이다. 개발용은 기간·용도 제한이 붙으므로, 방송에 쓰기 전에 vworld.kr 에서
   운영용을 신청해 갈아끼울 것 (마이페이지 → 오픈API 인증키 관리에서 종류·만료일 확인).
   **갈아끼우는 건 한 줄이다** — `config.js` 의 `SEARCH_KEYS.vworld` 하나뿐이고,
   엔드포인트·파라미터는 그대로다. 바꾼 뒤 `?v=` 를 올리지 않으면 GitHub Pages 캐시 때문에
   브라우저가 옛 `config.js` 를 계속 쓴다.
   만료되면 **검색이 조용히 죽는다** — 화면에는 "결과 없음" 으로만 보인다
   (콘솔에는 `[검색] VWorld 사용 불가:` 로 원인이 남는다).

1. **Google Places 키의 API 제한** — 키와 리퍼러 제한은 걸었고 폴백도 확인했다(2026-08-16).
   남은 것은 콘솔에서 **API 제한을 `Places API (New)` 하나로** 좁히는 것뿐이다. 2차 방어라
   급하지 않다 — 키가 새더라도 다른 유료 API 로 청구되지 않게 하는 장치다.
2. ~~`admin1.json` 40MB 줄이기~~ — 끝났다(2026-08-25). 나라별로 쪼개 온디맨드로 받는다.
   첫 로드 10.7 → 7.3MB. **좌표를 5자리로 줄이는 건 의미가 없었다** — 실측 10.7 → 10.3MB.
   gzip 이 반복되는 자릿수를 이미 압축한다. 크기를 줄이려면 꼭짓점을 줄이거나 나눠야 한다.
3. **스크린샷 회귀(Playwright)** — 지금 테스트는 jsdom 이라 **WebGL 렌더 결과를 못 본다.**
   위성 바다 뿌옇던 것, 색칠 삼각형 스파이크 둘 다 눈으로만 잡혔다. 유일하게 비어 있는 층.
4. **`main` 머지·배포** — 브라우저 확인은 끝났다(2026-08-16). 팀장 리뷰 후 merge 하면
   공개 사이트가 곧바로 3.0 이 된다.
5. **`--muted` 를 진하게 갈지 결정** — 지금 `#64748b` 은 카드 위 대비 3.95:1 로 작은 글씨
   기준(4.5:1)에 살짝 못 미친다. 보조 라벨(투명도·두께·점선·힌트) 전반에 쓰이는 색이라
   임의로 안 바꿨다. 진하게 가려면 `#5a6577` 정도면 넘긴다.
6. (선택) `recorder.js` 를 ES 모듈로 쪼개기. 그러면 `test/helpers/extract.js` 를 버리고
   `import` 로 바꿀 수 있다.
