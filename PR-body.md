> ## ⛔ 아직 merge 하지 마세요 — 국경선 문제 확인 중
>
> 국가 단위로 칠할 때 **국경선 표기에 방송 사고 위험이 있는 문제** 세 가지를 확인했습니다.
> 고치는 중이며, 아래 '국경선' 항목이 전부 해결되기 전에는 배포하면 안 됩니다.
>
> 1. **한강 하구 국경선이 강화·교동 위가 아니라 김포–강화 사이(염하)로 들어갑니다.**
>    `build-korea-border.mjs` 가 경기도·강원도에서만 선을 뽑는데 강화·교동·석모도는
>    인천광역시라서, 선이 126.517°E 에서 끊깁니다. 2.0 과 다른 모양입니다.
> 2. **국가 단위에서 선과 색칠이 어긋납니다.** 선은 국토부, 국가 색칠은 Mapbox 라
>    최대 3.1km 차이가 납니다. 시도 단위로 칠하면 맞습니다.
> 3. **Mapbox 가 갈도·장재도·무도·료도를 KOR 로 분류합니다.** 국가 단위로 대한민국을
>    칠하면 이 북한 섬 4개가 함께 칠해집니다. 행정구역 단위에서는 북한으로 제대로 나옵니다.
>
> 1·2 는 8월 14일 국경선 커밋부터, 3 은 Mapbox 데이터 자체의 오류입니다.

2.0 은 지도를 색칠하는 도구였습니다. 3.0 은 여기에 **카메라 녹화와 파일 내보내기**를 더해,
방송용 지도 그래픽을 한 화면에서 만들고 바로 뽑아낼 수 있게 했습니다.

커밋 35개. 빌드 단계는 없고 파일을 그대로 서빙하는 구조 그대로입니다.

## 새로 생긴 기능

**저장 및 내보내기** — 2.0 에 아예 없던 축입니다
- MP4 영상 녹화 (카메라가 움직이는 장면을 그대로)
- PNG / PSD / SVG
- 포맷·FPS·화질(Mbps) 조절, 캡처 영역 선택 (현재 창 / 1920×1080 / 1280×720)

**카메라 경로**
- 출발·도착 지점을 지정하면 그 사이를 움직이는 영상이 만들어집니다
- 경유지 최대 3곳, 이동 방식, 이동 중 줌아웃, 앞뒤 정지 시간
- 이동 경로를 선으로 표시 (모양·색·두께·점선)

**핀과 주석**
- 출발/경유/도착 핀, 위치 핀 (글자 크기·색·등장 시점)
- 그리기 모드 — 지도에 그린 선이 카메라 이동에 맞춰 영상에서 그려집니다

**지역 단위 확대**
| | 2.0 | 3.0 |
|---|---|---|
| 단위 | 국가, 시도 | 국가, 해외 행정구역, 시도, **시군구** |
| 개수 | 각 최대 3개 | 제한 없음 |
| 색 | 단위별 | **지역마다 색·불투명도 따로** |

**한국어 장소 검색** — Mapbox 는 한국 POI 가 사실상 비어 있어 국내 API(VWorld)를 붙였습니다.
못 찾으면 Google Places 로 넘어갑니다. 시도·시군구는 약칭으로도 찾습니다 (충북, 강원 등).

## 지도 정확도

- **새만금** — 국토부 데이터 어디에도 배정돼 있지 않아 빠져 있던 매립지를 전북에 반영했습니다.
  새만금호 수면은 제외했습니다 (통째로 채우면 바다를 칠한 것처럼 보입니다). 출처는 OSM(ODbL).
- **북쪽 국경선** — Mapbox 기본 국경선이 우리 행정경계와 최대 3.1km 어긋나 있어,
  국토부 시도 데이터에서 직접 뽑아 그리도록 교체했습니다.
- 시군구 경계선이 색칠에 덮여 사라지던 문제, 강·호수가 육지 위에 옅게 떠 보이던 문제,
  위성 스타일에서 바다가 뿌옇던 문제 해결.

## 품질 관리

테스트 68개를 새로 만들었고 푸시할 때마다 자동으로 돕니다.
사내 npm 이 막혀 있어 **의존성 없는 층(33개)** 과 **jsdom 이 필요한 층(35개)** 으로 나눴습니다.
로컬에서는 앞의 33개가 설치 없이 그대로 돌아갑니다.

## merge 전에 확인이 필요합니다

**merge 하면 공개 사이트가 곧바로 3.0 으로 바뀝니다.** 3.0 은 아직 배포된 적이 없어서
아래를 마친 뒤에 눌러 주시면 좋겠습니다.

**국경선 (배포 차단)**
- [ ] 한강 하구 — 강화·교동 위로 이어지게
- [ ] 국가 단위에서 선과 색칠 일치
- [ ] 갈도·장재도·무도·료도가 대한민국으로 칠해지지 않게

**나머지**
- [ ] 브라우저 확인 — 단색지형의 시군구 경계선·강 색, 위성 바다, 연천·양구 국경선
- [ ] `경포대해수욕장` 검색 (Google 폴백이 실제로 도는지)
- [x] 모노톤 스타일 투영
- [x] `.DS_Store` 정리

작업 맥락과 결정의 근거는 `CLAUDE.md`, 테스트가 막고 있는 실제 사고 목록은 `test/README.md` 에 있습니다.
